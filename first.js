// js 1995 devloped and mr. brandon eich and there are created in only 10 days;
//  name =mocha ;
// ecma script registred name  (european computer manufacture association);
// js is a dynamiclly type language;
// js is a client side and server side scripting language;
// js is very light weighted language ;
// it is mostly famous in 2014-15 in ecma es6 update ;
// it is firstly used in server site in 2008-09 nodejs language in used ;
// The newest version of JavaScript is called ECMAScript 2024 or 15th ES2024 for short. It came out in June 2024


 // 'use strict' 2016 ;
 // let and conts, arrow function;
// Data Type ;
// Three two of data types ;
//1. primitive data types;
//  1. number = integer & floating-point numbers
//  2. boolean = true or false 
// 3.string = a sequence of numerical;
// 4. Null =
// 5. undefined=
// 2. non-primitive data types  1. object 2.array;
// Number , String , undefined , null , BigInt, Symbol;
// var , let,  keyword ;

// Scope 
//  scope are use in the curly brases{};
// scope refers to the accessibility of variables , function ,and objects ;

// let are reasign and not are redecleared;
// var are not reasign and there  are redecleared;
// VAR are function scope;
// LET or CONS are blocked scope;

// HOSTING;
// 
// redecleration=var
// reassignment = var or let

// **Hoisting
// jitna bhe var key word sa variable decliration ha wo unka function ka andar top pa ja ka baith jata ha
// Hoisting are use only var
// its is donot work in lets and cons



// ** Java Script ia a synchronus is is works in fifo or Quee
// ** function are most use in componey are arrow function
// ** two key are lounched in 2015 es6 update let, const;
// ** Why lets const are launched 

// HW
// find es current using update or version 
// ans= ECMAScript 2024 or 15 th

// hw
// Scope,Hosting 

// * Scope
// Scope are use in the curly brases {}
//  scope refers to the accessibility of variables, functions, and objects in different parts of your code

// VAR are function scope
  
// let or cons are blocked scope



// *********************************************************BOM***********************************************************
// Browser Object Model

// `.Intrecting with the browsing window

// first we write window.alert("hi")
// method; 
// alert = msg display/show krta hai; 
// confirm = do buttons deta hai, true /false / ok and chanle; 
// prompt = input lene ke liye and feedback lene ke liye;

// hw ;
// user sa prompt ma do value mangane ha ;
// int no 1;
// int no 2;

// then add the ans;

//  ** parseInt ,parseFloat ,Number it is convet string into number;

// **********************************************************DOM**************************************************************************************
//   document object model;
// it is a tree type of structure in  dom  it be can changed in html and access of run in java script;

// hw;
// 2 ya 3 button ushka click karna pa alert aiya this is 1 fn 2

// 3function 3 button;

// string methods ;
// length() , slice(), concat(), substring(), replace(), toUpper(), replace(), charAt(), split(), indexOff() ,trim();
// replaceAll();
// java script is a synchronous language but kuch method hai hn jo use asynchronous beive krata hai;
// set timeout = setTimeout() is used to execute a function once a specifed time delay (in miliseconds);
//   syntax:-     setTimeout(function,delay);

// setInterval() = is used to execute a function repeatedly at specified intervals (in miliseconds).
//     syntax:-      setInterval(function,interval);