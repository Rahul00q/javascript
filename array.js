// ---------------------------Array Methods;-------------------------------;
 //  1. length() = returns the number of elements in the array;
 //  2. pop() = remove the last element;
 //  3. push() = add a new element to the end;
 //  4. shift() = remove the frist elements;
 //  5. unshift()= add a new element to the beginning; 
 //  6. slice() =
 //  7. concAt() =
 //  8. spslice() =
 // let fruits=["Apple","Mango","Banana","Grapes"]    //  Array is nonpremitive;
//  console.log(fruits[2]);    // to access array elements ;
//  console.log(fruits.length); // to take length;
//  fruits.pop();        //     remove last elements from array;
//  console.log(fruits);


//  fruits.push("pineapple");     adds elements at last;
//  console.log(fruits);

    // fruits.shift();      remove the frist elements;
    // console.log(fruits);
    // fruits.unshift("aaa");   add a new elements to the beginning and frist;
    // console.log(fruits);

let a= ["rahul", "meena"];
let b= ["prem", "kumar"];
console.log(a.concat(b));
